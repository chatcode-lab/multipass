# Used only by `npm run data:bootstrap`; store real values in an ignored .env or
# inject them into the process environment. Never commit the token.
SYNC_ENDPOINT=https://multipass-data-sync.example.workers.dev
SYNC_TOKEN=replace-locally

